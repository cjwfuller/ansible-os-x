---
base_osx:
  main:
    taps:
      - homebrew/dupes
      - phinze/cask

    packages:
      - ansible
      - brew-cask
      - cloc
      - git
      - node
      - vim
      - wget
      - zsh

    casks:
      - iterm2
      - virtualbox
      - vagrant
      - evernote

    python_packages:
      requirements:
        - Jinja2
        - flake8
